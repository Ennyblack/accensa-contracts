[package]
name = "receipt-anchor"
version = "0.2.0"
edition = "2021"
rust-version.workspace = true

[dependencies]
soroban-sdk.workspace = true

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[lib]
crate-type = ["lib"
             ,
             "cdylib"
]
